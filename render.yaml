services:
  - type: web
    name: family-cost
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node src/backend/server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
